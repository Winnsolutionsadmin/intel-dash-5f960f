# UPSTREAM SOURCE OF TRUTH

This repo is a PUBLIC downstream mirror of a published artifact.
The canonical source auto-loads via the operator's local ~/.claude registry and is deliberately
not committed here, because this repository is public.

Edit upstream and re-publish.
